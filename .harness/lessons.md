# Lições — fricções observadas

Append-only. Escrito por `harness lesson`. Quem fecha um item é o HUMANO,
revisando em cadência própria — o agente anota, não aplica.
- [ ] re-prova incremental verde nao recarimba a evidencia da tarefa antiga: ela continua stale e o harness finish cobra um harness verify manual → avaliar se a re-prova verde deve regravar files_hash da evidencia da tarefa re-provada
- [ ] guard nega git switch/pull isolados mas deixou passar em chamada composta, e nao ha caminho sancionado de volta para a branch de contrato → tornar o deny de git switch deterministico e abrir retorno para contract/<slug> ativo
- [ ] session_permissions.FIXED_HARNESS_SEQUENCES esta desatualizado: nao tem blind, finish, budget, reconcile, decide, lesson, task nem pr-draft, e o comentario ainda diz espelhar o guard → derivar a lista do mesmo lugar que o guard, ou um teste que compare as duas listas -- o mesmo mecanismo que ja pega verbo esquecido no guard
- [ ] veredito da camada 3 e invalidado pelas proprias observacoes nao-bloqueantes do verificador: agir sobre elas muda arquivo do contrato e o veredito vira stale, exigindo nova passada → avaliar veredito escopado por tarefa, ou um modo de re-julgamento so do delta desde o veredito anterior
- [ ] contagem de casos no README nao tem teste que a trave: errei o numero duas vezes seguidas na mesma demanda, e so o verificador cego pegou → teste que compara o numero do README com pytest --collect-only, como test_version_sync ja faz com a versao
- [ ] _GIT_LOCAL_ALLOW do session_permissions e mantido a mao e nao tem git branch --show-current, que o guard libera -- mesma classe da lista de verbos, do lado do git → derivar a superficie git local da mesma fonte que o guard usa, como HARNESS_CLI_VERBS passou a ser
- [ ] numeros historicos do README (1008/724 casos) escapam do padrao (\d{3,}) casos so porque ha quebra de linha entre o numero e a palavra: reflow do paragrafo faria o teste falhar num numero legitimamente historico → distinguir afirmacao de historico por marcador explicito, ou reescrever os numeros historicos numa forma que o padrao nunca alcance
- [ ] tarefa acoplada com o MESMO verify_cmd fica de fora da re-prova (proof == current_proof), entao ela nao e recarimbada e o finish cobra harness verify a mao -- mas a prova dela literalmente acabou de rodar, como verify da tarefa nova → quando a prova acoplada e identica a que acabou de passar, recarimbar as feature_ids acopladas sem re-executar nada: o veredito ja existe
- [ ] verbo novo de CLI so passa pelo guard depois de um compile-session: o proprio contrato que cria o verbo tem seu primeiro uso negado, e a mensagem de deny sugere extra_allowed_commands em vez do recompile → o guard sugerir compile-session quando o verbo negado ja existe em HARNESS_CLI_VERBS do pacote instalado mas nao no hook compilado
- [ ] o teste de numeros da documentacao nao ve numero separado da palavra por tag HTML: o KPI da arquitetura visual dizia 32 modulos, 20 subcomandos e 908 testes com a suite 100% verde, porque o padrao exige numero e palavra adjacentes → remover as tags antes de casar o padrao nos arquivos .html, ou trocar (\d+) palavra por (\d+)[^a-zA-Z0-9]{0,80} palavra
- [ ] tabela de modulos da arquitetura visual tem 32 linhas enquanto a pilula ao lado diz 39 modulos, e faltam 8 (attempts, blind, budget, findings, health, reconcile, regression, spine): o teste de numeros confere a pilula, nao o inventario → derivar as linhas da tabela de src/harness/*.py, ou um teste que compare a contagem de linhas do #modtable com o numero declarado
- [ ] com o kill-switch ativo o SessionStart devolve so o banner e nunca chama build_context, entao a sessao desativada perde os checks de ferramenta e de governanca do health check → avaliar se o banner de desativado deve carregar tambem a secao do health, ja que ambiente quebrado continua sendo verdade com o harness desligado
- [ ] timeout do _health_section (120s) nao conversa com o _PROBE_TIMEOUT por modulo (60s): tres verify_cmd na forma -m podem estourar o teto do hook e o aviso some em silencio → derivar o timeout do hook da soma dos probes, ou baixar o probe para caber num teto declarado
- [ ] probe de import roda com o cwd no sys.path, entao um modulo de nome valido presente na arvore e importado (e executa) na abertura da sessao -- concorda com o que o verify faria, mas roda sozinho, sem humano olhando a branch → avaliar -P/-I no argv do probe: passaria a perguntar pelo modulo INSTALADO, o que muda a semantica do check e por isso e decisao de desenho, nao conserto
- [ ] ninguem valida a FORMA do verify_cmd: runtime_audit so exige que o campo exista, entao um 'python -m pathlib;print(1)' vira contrato compilado e o health cala sobre ele de proposito (o unico veredito dele e infra, e contrato malformado e falha estrutural) → compile-contract ou audit-runtime julgarem a forma do verify_cmd -- os dois ja julgam o feature_list.json e a reacao natural deles e corrigir o contrato, nao parar
- [ ] veredito da camada 3 invalidado pelas proprias observacoes aconteceu de novo: tres passadas neste contrato, cada correcao dos achados mudou arquivo do contrato e exigiu re-julgamento (segunda ocorrencia da licao ja aberta) → re-julgamento so do delta desde o veredito anterior: SendMessage ao mesmo verificador com a lista de correcoes funcionou bem na pratica, e cabe virar passo do lifecycle em vez de improviso
- [ ] Editei src/harness/lifecycle.py e a suite ficou verde, mas AGENTS.md e .harness/LIFECYCLE.md (os artefatos que a sessao realmente le) continuaram com o texto antigo -- a instrucao nova ficou inerte no repo e so o verificador cego pegou → Depois de mexer em qualquer render_* de artefato compilado, rodar harness compile-session ANTES de verificar a tarefa; a guarda mecanica agora e tests/test_lifecycle.py:266
- [ ] O pacote do verificador cego lista spec.md e Plans.md em 'onde olhar' de uma tarefa e nas linhas seguintes proibe abri-los -- o gerador manda ler o que ele mesmo veta → build_package filtrar dos files[] os caminhos que a secao de proibidos ja nomeia
- [ ] test_version_sync nao cobria a entrada de CHANGELOG, e test_docs_placar tinha janela sem limite superior: as duas guardas nomeavam propriedade que nao verificavam -- so o verificador cego pegou → ao escrever guarda de documentacao, provar que ela reprova o estado ANTERIOR antes de fechar a tarefa
- [ ] progress.md fica com AGUARDANDO VOCE depois do destrave pelo caminho 2 (watch): a linha so e reescrita por unblock ou verify, e o SessionStart injeta esse arquivo -- a sessao seguinte abre lendo uma espera que nao existe mais → resolver o bloqueio de forma nao-lazy: quem detecta que o watch mudou apaga o json e sincroniza a coluna, ou o render do progresso passa a derivar de read_block em vez do texto gravado
- [ ] statusline reimplementa a escolha da feature corrente lendo so feature_list.json e aponta a fatia parada como atual, contradizendo panel.collect_state como fonte unica dos renders → statusline consultar .harness/blocks/ na mesma leitura magra do resto, ou derivar a feature corrente da mesma funcao que o placar usa
- [ ] needs com pipe quebra a tabela do progress.md: o texto cru e interpolado na coluna de status e update_progress_status exige 5 campos no split → escapar pipe ao gravar a coluna de status, ou o update recusar valor que contenha o separador
- [ ] finish pula a checagem de evidencia quando ha bloqueio: o ramo do bloqueio precede o elif de passes, entao fatia marcada como passando com bloqueio vivo escapa de evidence_missing/stale → avaliar reportar os dois bloqueadores em vez de um elif -- bloqueio e evidencia sao perguntas independentes sobre a mesma fatia
