> 🏁 DEMANDA FECHADA — 2026-07-16

# BACKLOG DE EXECUÇÃO - CLAUDE CODE
# Fase 2 do ROADMAP.md — Execução Autônoma no "Raio de Impacto"
# Origem: ROADMAP.md (seção "Fase 2", linhas ~130-206). Gerado 2026-07-16 via plan-to-backlog.
# Pré-requisito: Fase 1 fechada (ROADMAP-fase1.backlog.md, evidência em
# tests/e2e/evidence/fase1-outcomes-verification.md) — este backlog consome
# `src/harness/contract.py` (feature_list.json), `src/harness/analyzer.py`
# (repo-profile.json) e `src/harness/compiler.py` (padrão de merge não-destrutivo
# em .claude/settings.json + .harness/compiled-state.json) como estão, sem alterá-los.
#
# Validação global ao fechar: $env:PYTHONPATH = "src"; python -m pytest tests -q
# (suíte pré-existente + novos testes desta fase, zero falhas)
# [SUBAGENTE 09] é opt-in (HARNESS_E2E_DOGFOOD=1) e NÃO entra nesse gate — mesmo
# padrão de tests/e2e/test_headless.py e do [SUBAGENTE 08] da Fase 1.
#
# Bootstrap (rodar uma vez, antes do primeiro subagente):
#   pip install -e ".[dev]"
#
# ✅ Revisado por reflect (plan-critic, Sonnet) + llm-as-judge (Sonnet, independente)
# em 2026-07-16. 5 achados confirmados e corrigidos diretamente neste arquivo antes da
# execução: (1) bypass do runtime floor sem contrato ativo — reordenado, floor checa
# primeiro; (2) divergência não-marcada do "state/lifecycle no yaml" — agora anotada
# como [design próprio] em SUBAGENTE 03; (3) colisão de compiled-state.json com o
# mecanismo antigo — módulos novos usam compiled-state-session.json, arquivo próprio;
# (4) colisão do hook antigo guard_tests.py com boundary_guard.py — install_boundary_guard
# agora remove a entrada antiga; (5) colisão rede×lifecycle (install de deps negado pelo
# guard) — Bash(<install_cmd>) adicionado à superfície allow. Bug adicional (.get inseguro
# com None explícito) corrigido em SUBAGENTE 01 e 04.
#
# 🗺️ Mapa de Dependências dos Subagentes:
#   - 🟢 Fase A: [SUBAGENTE 01] session_permissions.py, [SUBAGENTE 02] boundary_guard.py,
#     [SUBAGENTE 03] lifecycle.py, [SUBAGENTE 04] templates.py, [SUBAGENTE 05] session_start.py
#     — TODOS em paralelo: cada um cria um módulo novo + um arquivo de teste novo,
#     disjuntos entre si, nenhum toca em cli.py, compiler.py ou contract.py.
#   - 🟡 Fase B: [SUBAGENTE 06] CLI `compile-session` + skill `plan` — depende de
#     01, 02, 03, 04 e 05 (é o único ponto que importa as 5 funções `install_*`/`compile_*`
#     e por isso é sequencial: evita 5 tarefas paralelas editando cli.py ao mesmo tempo).
#   - 🟠 Fase C: [SUBAGENTE 07] E2E sintético do fluxo combinado — depende de 06
#     (precisa do subcomando `compile-session` existir de ponta a ponta).
#   - 🏁 Fase D: [SUBAGENTE 08]... não existe — ver abaixo, é o 09.
#   - 🏁 Fase D: [SUBAGENTE 09] dogfood real, AMPLIA `tests/e2e/test_contract_dogfood.py`
#     da Fase 1 — depende de 07, roda por último, sequencial, opt-in.

### [SUBAGENTE 01] - Contrato → superfície de permissions da sessão
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Criar `src/harness/session_permissions.py`, que lê o contrato compilado (`.harness/feature_list.json`) e o profile (`.harness/repo-profile.json`) e produz/mescla a lista `allow` do `.claude/settings.json` com a superfície ENUMERADA que o lifecycle usa — nunca genérica.
- **📂 Escopo de Arquivos:**
  - Ler: `src/harness/contract.py` (formato exato de `feature_list.json`: `{"features": [{"id","desc","files":[...],"verify_cmd","depends":[...],"passes"}]}`), `src/harness/analyzer.py` (formato de `.harness/repo-profile.json`: `extras.lint_command`/`extras.typecheck_command`/`extras.build_command`, cada um `{"value","evidence","confidence"}`), `src/harness/compiler.py` (só como REFERÊNCIA do padrão de merge não-destrutivo via `.harness/compiled-state.json` em `_merge_settings`/`_load_state`/`_write_state` — não importe nem modifique essas funções privadas), `ROADMAP.md` (seção "Fase 2", mecânica de permissions)
  - Modificar (criar): `src/harness/session_permissions.py`, `tests/test_session_permissions.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, crie `src/harness/session_permissions.py` (stdlib + json, docstring PT-BR, funções puras separadas de I/O — mesmo estilo de `contract.py`/`analyzer.py`). Função pura `render_session_permissions(feature_list: dict, profile: dict | None) -> dict[str, list[str]]` retornando `{\"allow\": [...]}` com: (1) `Edit(<file>)` e `Write(<file>)` para CADA arquivo na união de `files[]` de TODAS as features de `feature_list['features']` (sem duplicar); (2) `Bash(<verify_cmd>)` literal para cada `verify_cmd` distinto das features; (3) se `profile` não for `None`, `Bash(<valor>)` para `profile['extras']['lint_command']['value']`, `profile['extras']['typecheck_command']['value']` e `profile['extras']['build_command']['value']` quando essas chaves existirem (use `(profile.get('extras') or {}).get(...)` — nunca `profile.get('extras', {})` sozinho, porque a chave pode existir com valor `None` explícito, não só ausente); (3.5) `Bash(<comando de instalação>)` derivado de `(profile.get('package_manager') or {}).get('value')` quando presente (mapeie o `value` — `npm`/`pnpm`/`yarn`/`uv`/`poetry` — pro comando real: `npm ci`, `pnpm install --frozen-lockfile`, `yarn install --frozen-lockfile`, `uv sync`, `poetry install`; omita se ausente/None) — isto resolve a colisão rede×lifecycle do ROADMAP (passo 2 do ciclo de 16 passos roda `init.sh`, que instala deps; sem essa entrada, a sessão ficaria sem permissão pro próprio primeiro passo do lifecycle); (4) git local fixo: `Bash(git status)`, `Bash(git log*)`, `Bash(git diff*)`, `Bash(git add*)`, `Bash(git commit*)`. NUNCA inclua `git push`, comandos de rede (curl/wget/npm publish/pip upload etc.) ou qualquer coisa fora desta lista — são o runtime floor do ROADMAP (Fase 2, 'nunca vira allow') e ficam de fora por omissão, não por checagem explícita. Função `compile_session_permissions(target_dir: Path) -> Path`: lê `target_dir/.harness/feature_list.json` (se ausente, levante `FileNotFoundError` com mensagem clara pedindo para rodar `compile-contract` primeiro) e `target_dir/.harness/repo-profile.json` (se ausente, trate como `profile=None`, não é erro). Chama `render_session_permissions`, e mescla o resultado em `target_dir/.claude/settings.json` (`permissions.allow`) SEM tocar nos buckets `ask`/`deny` e SEM apagar entradas manuais ou entradas gerenciadas por OUTRO mecanismo (ex.: as de `compiler.py`/`harness.yaml`). Implemente seu PRÓPRIO merge não-destrutivo (não importe as funções privadas de `compiler.py` — replique o padrão, mas em ARQUIVO PRÓPRIO: registre em `target_dir/.harness/compiled-state-session.json` — NUNCA em `.harness/compiled-state.json`, porque `compiler.py::_write_state` reconstrói esse arquivo do ZERO a cada `harness compile` e apagaria silenciosamente qualquer chave nova que não seja `managed_permissions`/`managed_hook_commands` — sob a chave `\"managed_session_permissions\"` a lista de regras que VOCÊ escreveu da última vez; ao recompilar, remova do `permissions.allow` só as regras que estavam nessa chave e ainda não estão na nova saída, e insira as novas — se `compiled-state-session.json` já existir com outras chaves (de `boundary_guard.py`/`session_start.py`, escritas pelas tarefas irmãs), preserve-as intactas, leia o JSON existente e só adicione/atualize a sua chave). Grave `settings.json` com `indent=2, ensure_ascii=False` e newline final, criando `.claude/` se preciso. Crie `tests/test_session_permissions.py`: `render_session_permissions` com 2 features de arquivos diferentes gera Edit/Write para os 2 sem duplicar; `verify_cmd` repetido em 2 features vira 1 `Bash(...)` só; profile com `extras` populado adiciona lint/typecheck/build; profile `None` não quebra; saída NUNCA contém 'git push' nem 'curl'/'wget' em nenhum cenário; `compile_session_permissions` sem `feature_list.json` levanta `FileNotFoundError`; compilar sobre um `settings.json` com uma regra `allow` manual do usuário PRESERVA essa regra; recompilar com uma feature removida do contrato REMOVE a permissão daquele arquivo específico (mas preserva a manual); recompilar preserva chaves prévias não-relacionadas em `compiled-state-session.json` (ex.: simule uma chave de outra tarefa, tipo `boundary_guard_hook_command`, pré-existente e confirme que sobrevive); `render_session_permissions` com `profile['package_manager'] = {\"value\": \"npm\", ...}` gera `Bash(npm ci)`; profile com `package_manager` valor `None` explícito (chave presente, valor `None`) não lança exceção e não adiciona comando de instalação. Não toque em `cli.py`, `compiler.py` nem `contract.py`. Não faça refatoração fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests/test_session_permissions.py -q` — verde
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — suíte inteira verde (nenhuma regressão)
  - [ ] `python -m ruff check src/harness/session_permissions.py tests/test_session_permissions.py` — limpo

---

### [SUBAGENTE 02] - `boundary_guard.py`: dispatcher único de fronteira (Edit/Write/Bash)
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Criar `src/harness/boundary_guard.py` — o ÚNICO hook PreToolUse que cobre Edit/Write e Bash ao mesmo tempo, decidindo `allow`/`deny` a partir da superfície do contrato ativo (`.harness/feature_list.json`), substituindo o padrão de N guards por ação. Inclui a proteção contra enfraquecimento de testes (arquivo de teste só editável se declarado em `files[]` de alguma tarefa).
- **📂 Escopo de Arquivos:**
  - Ler: `src/harness/compiler.py` (`_render_guard_tests`/`_render_guard_test_runner` e `_hook_entry` — padrão de hook standalone stdlib-only embutido como string, e formato `{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"permissionDecision\": ..., \"permissionDecisionReason\": ...}}`; NÃO importe nada de `harness.*` dentro do hook gerado — hooks rodam fora do pacote), `src/harness/verification/tdd_loop.py` (`_glob_to_regex` — copie o MESMO algoritmo inline no texto do hook gerado, já que o hook não pode importar a lib), `tests/test_compiler.py` (função `_run_hook`, padrão de teste: subprocess com stdin JSON, lê `hookSpecificOutput` do stdout), `ROADMAP.md` (seção Fase 2, itens do boundary_guard e da proteção de testes)
  - Modificar (criar): `src/harness/boundary_guard.py`, `tests/test_boundary_guard.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, crie `src/harness/boundary_guard.py` (docstring PT-BR). Função `render_boundary_guard() -> str` retorna o CÓDIGO-FONTE (string) de um script Python standalone (stdlib apenas: json, re, subprocess, sys — SEM `import harness`) que implementa o dispatcher PreToolUse único para os matchers Edit, Write e Bash. Comportamento do script gerado, lendo o payload JSON do stdin (`tool_name`, `tool_input`, `cwd`) e resolvendo `file_path`/`command` do jeito que `compiler.py` já faz (path relativo ao `cwd`, barras normalizadas):
  > **ORDEM DE AVALIAÇÃO OBRIGATÓRIA — o passo 1 (runtime floor) roda SEMPRE PRIMEIRO, antes de qualquer outro passo, inclusive antes de checar se existe contrato ativo. Isto não é uma sugestão de estilo: uma implementação que decida 'sem contrato → allow' antes de checar o floor abre uma falha de segurança real (git push/segredos liberados em qualquer repo sem feature_list.json). Rejeite qualquer estrutura de código (if/elif em cascata, early return) que permita o floor ser pulado.**
  > 1. RUNTIME FLOOR — avalie ANTES de tudo, incondicionalmente (nunca vira allow, regra dura do ROADMAP, vale COM ou SEM contrato ativo): comando Bash cuja sequência de tokens contém `git push`; comando Bash que bate com `curl`, `wget`, `npm publish`, `pip upload`, `twine upload`, `gh release` (mesmo casamento por sequência de tokens); Edit/Write cujo path resolvido termine em `.env`, `.pem`, `id_rsa` ou tenha `credentials` no nome do arquivo. Se bater, retorne `deny` IMEDIATAMENTE — razão deve citar explicitamente 'runtime floor' — e NÃO avalie nenhum passo seguinte, mesmo que `.harness/feature_list.json` não exista.
  > 2. Carregue `.harness/feature_list.json` relativo ao `cwd` (se não existir, e o passo 1 não bateu: SEM contrato ativo → responda `allow` com razão 'sem contrato ativo — boundary_guard não gateia fora de uma sessão de contrato'; isso não pode quebrar desenvolvimento normal do plugin em repos sem contrato).
  > 3. Carregue `.harness/repo-profile.json` relativo ao `cwd` se existir (opcional — `test_glob` vem de `profile['test_glob']['value']` quando presente).
  > 4. Superfície do contrato: `allowed_files` = união de `files` de todas as features; `allowed_bash` = todos os `verify_cmd` + (se profile presente) `extras.lint_command/typecheck_command/build_command` (`.value`) + `Bash(<comando de instalação>)` derivado de `profile['package_manager']['value']` quando essa chave existir e não for `None` (ex.: valor `"npm ci"` ou `"uv sync"` — isso resolve a colisão rede×lifecycle do ROADMAP: o passo 2 do ciclo de 16 passos roda `init.sh`/`init.ps1`, que instala deps; sem essa entrada na superfície, o boundary_guard negaria o próprio primeiro passo do lifecycle) + comandos git locais fixos: `git status`, `git log`, `git diff`, `git add`, `git commit` (casar por SEQUÊNCIA de tokens, replicando `_has_runner_sequence`/`SHELL_SPLIT` de `compiler.py::_render_guard_test_runner` — copie a lógica de tokenização, não invente outra).
  > 5. Proteção de teste: se `test_glob` do profile existir e o path resolvido casar com o regex traduzido do glob (algoritmo idêntico a `_glob_to_regex`, copiado inline), o arquivo SÓ é `allow` se ele TAMBÉM estiver em `allowed_files` (ou seja, alguma tarefa do contrato declara esse teste em `files[]`); caso contrário `deny` com razão 'arquivo de teste protegido: nenhuma tarefa do contrato ativo declara este arquivo em files[] — enfraquecimento de teste fora do escopo aprovado'.
  > 6. Edit/Write fora da proteção de teste: `allow` se o path resolvido estiver em `allowed_files`; senão `deny` com razão 'arquivo fora da superfície do contrato ativo (nenhuma tarefa declara este path em files[]); replaneje via /harness-creator:plan se o escopo mudou'.
  > 7. Bash: `allow` se a sequência de tokens do comando bater com algum item de `allowed_bash` OU com um dos comandos git locais fixos do item 4; senão `deny` com razão 'comando fora da superfície compilada do contrato (verify_cmd/lint/typecheck/build/install/git local); replaneje via /harness-creator:plan se precisar de outro comando'.
  > 8. Tudo que não for Edit/Write/Bash (não deveria acontecer, já que o matcher é só esses três): `allow` por padrão, razão 'ferramenta fora do escopo do boundary_guard'.
  > **AJUSTE PÓS-REVISÃO (reflect + judge validaram):** grave o estado gerenciado deste hook em `target_dir/.harness/compiled-state-session.json` (arquivo PRÓPRIO, não `.harness/compiled-state.json`) — o `compiler.py::_write_state` do mecanismo antigo (v0.10.0) reconstrói `compiled-state.json` do ZERO a cada chamada (só conhece as chaves `managed_permissions`/`managed_hook_commands`), então rodar o `harness compile` antigo depois de `compile-session` apagaria silenciosamente qualquer chave nova gravada lá. Use um arquivo de estado separado para os módulos desta fase (`session_permissions.py`, `boundary_guard.py`, `session_start.py` — as 3 tarefas que fazem merge não-destrutivo devem todas ler/escrever esse MESMO arquivo `compiled-state-session.json`, cada uma sob sua própria chave).
  > **REMOÇÃO DO HOOK ANTIGO DE PROTEÇÃO DE TESTE:** `install_boundary_guard` também deve REMOVER do `hooks.PreToolUse` qualquer entrada cujo `command` bata com o hook antigo `guard_tests.py` gerado por `compiler.py` (script cujo path termina em `guard_tests.py`) SE ela existir — o `boundary_guard.py` novo já cobre a proteção de teste (passo 5) de forma mais precisa (por tarefa do contrato, não por política estática); manter os dois ativos ao mesmo tempo faz o hook antigo disparar `ask` (auto-negado em modo headless) para o MESMO Edit que o novo já liberou por `allow`, quebrando a promessa de "zero prompts" da Fase 2 (`ROADMAP.md:141-142` — "os bloqueios duros do design anterior são removidos dinamicamente pela compilação do próprio contrato"). Não mexa em outras entradas de `hooks.PreToolUse` (ex. `guard_test_runner.py`, se ele não for especificamente sobre proteção de teste editável).
  > Função `install_boundary_guard(target_dir: Path) -> Path`: grava `target_dir/.harness/hooks/boundary_guard.py` com o conteúdo de `render_boundary_guard()`, e registra o hook em `target_dir/.claude/settings.json` (`hooks.PreToolUse`, matcher `\"Edit|Write|Bash\"`, `command: 'python \"<path absoluto de boundary_guard.py>\"'` — mesmo formato de `_hook_entry` de `compiler.py`, mas reimplementado aqui, sem importar `compiler.py`). Merge não-destrutivo: registre o command gerenciado sob a chave NOVA `boundary_guard_hook_command` em `.harness/compiled-state-session.json` (NUNCA `.harness/compiled-state.json` — ver AJUSTE PÓS-REVISÃO acima; preservando outras chaves já existentes no arquivo, ex. `managed_session_permissions` de outra tarefa); ao recompilar, remova do `hooks.PreToolUse` só a entrada cujo command bate com o valor antigo dessa chave, e insira a nova; NUNCA remova entradas de hook que não sejam suas, EXCETO a entrada antiga de `guard_tests.py` (ver REMOÇÃO DO HOOK ANTIGO acima — essa é removida de propósito); preserve `guard_test_runner.py`/outras entradas não relacionadas à proteção de teste. Crie `tests/test_boundary_guard.py` seguindo o padrão de `_run_hook` de `tests/test_compiler.py` (subprocess real do script gerado, payload JSON no stdin, ler `hookSpecificOutput` do stdout): sem `feature_list.json` → `allow`; Edit num arquivo listado em `files[]` de uma feature → `allow`; Edit num arquivo NÃO listado → `deny`; Bash com o `verify_cmd` exato de uma feature → `allow`; Bash `git status`/`git add .`/`git commit -m x` → `allow`; Bash `git push` (e variantes com metacaracteres, tipo `git push && true`) → SEMPRE `deny`, mesmo com contrato ativo cobrindo tudo; Bash `curl https://x` → `deny`; Edit em `.env` → `deny` mesmo se `.env` estiver em `files[]` (floor é mais forte que o contrato); arquivo de teste (`test_glob` casando) declarado em `files[]` de uma feature → `allow`; arquivo de teste casando o glob mas AUSENTE de `files[]` → `deny` citando enfraquecimento de teste; Bash arbitrário não relacionado (`echo oi`) → `deny`; **CRÍTICO (achado da revisão): SEM `feature_list.json` no diretório (nenhum contrato ativo), Bash `git push origin main` → AINDA `deny` (o floor roda antes do 'sem contrato → allow'); SEM `feature_list.json`, Edit em `.env` → AINDA `deny`** (prova de que o passo 1 nunca é pulado pelo passo 2); `install_boundary_guard` chamado num `.claude/settings.json` que já tem o hook antigo `guard_tests.py` registrado em `hooks.PreToolUse` → essa entrada é REMOVIDA após a instalação (só sobra a entrada do `boundary_guard.py`); profile com `package_manager` valor `None` explícito não quebra `allowed_bash` (trate como ausente). Não toque em `cli.py`, `compiler.py` nem `contract.py`. Não faça refatoração fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests/test_boundary_guard.py -q` — verde
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — suíte inteira verde
  - [ ] `python -m ruff check src/harness/boundary_guard.py tests/test_boundary_guard.py` — limpo

---

### [SUBAGENTE 03] - `lifecycle.py`: Agent Session Lifecycle de 16 passos no AGENTS.md
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Criar `src/harness/lifecycle.py` — compila o ciclo de 16 passos do ROADMAP como bloco gerenciado ADICIONAL no `AGENTS.md` (delimitadores próprios, não colidem com o bloco de `compiler.py`), com progressive disclosure: bloco fino no AGENTS.md aponta para um arquivo de detalhe.
- **⚠️ Divergência deliberada do texto do ROADMAP.md (achado da revisão plan-critic + judge):** `ROADMAP.md` linha ~198 lista a entrega como "seções `state`/`lifecycle` no yaml" (ou seja, dentro de `harness.yaml`/`HarnessConfig`, em `src/harness/config.py`). Esta tarefa implementa **[design próprio]** via módulo Python + bloco em `AGENTS.md` + arquivo `.harness/LIFECYCLE.md`, SEM tocar em `config.py`/`HarnessConfig` — mais simples e não exige estender o schema do `harness.yaml` pra algo que é essencialmente texto/instrução (não uma política estruturada como `governance`/`sandbox`). Documente essa escolha no docstring do módulo (uma linha citando que diverge do texto literal do roadmap por simplicidade, igual ao padrão de divergências marcadas em `ROADMAP.md` para outras fases).
- **📂 Escopo de Arquivos:**
  - Ler: `src/harness/compiler.py` (`_write_agents_block`, `AGENTS_BEGIN`/`AGENTS_END` — padrão de substituição idempotente de bloco delimitado; NÃO reuse essas constantes, o bloco desta tarefa é outro, adjacente), `ROADMAP.md` (seção "Fase 2", lista completa dos 16 passos do lifecycle, linhas ~183-195)
  - Modificar (criar): `src/harness/lifecycle.py`, `tests/test_lifecycle.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, crie `src/harness/lifecycle.py` (docstring PT-BR). Constantes `LIFECYCLE_BEGIN = \"<!-- harness:lifecycle:begin -->\"` e `LIFECYCLE_END = \"<!-- harness:lifecycle:end -->\"` (delimitadores PRÓPRIOS, diferentes de `AGENTS_BEGIN`/`AGENTS_END` de `compiler.py`, para o bloco desta tarefa conviver no mesmo AGENTS.md sem conflito). Função `render_lifecycle_block() -> str` retorna um bloco curto delimitado por essas constantes, com os 16 passos do ROADMAP.md (Fase 2) como lista numerada COMPACTA (uma linha por passo, resumindo: 1. ler AGENTS.md; 2. rodar init.sh/init.ps1 (deps + health check); 3. ler claude-progress.md; 4. ler feature_list.json; 5. checar git log; 6. escolher exatamente UMA feature pendente; 7-11. implementar e verificar (rodar verify_cmd, autocorrigir até passar); 12-16. atualizar claude-progress.md, marcar feature concluída, documentar o que ficou quebrado se houver, commit só em estado retomável, deixar working tree limpo) e terminando com a linha 'Detalhe de cada passo: ver `.harness/LIFECYCLE.md`.' (progressive disclosure — não repita o detalhe completo no AGENTS.md). Função `render_lifecycle_detail() -> str` retorna o conteúdo completo de `.harness/LIFECYCLE.md`: um parágrafo por passo (pode reaproveitar a prosa do ROADMAP.md Fase 2), explicando o objetivo de cada um. Função `install_lifecycle(target_dir: Path) -> tuple[Path, Path]`: (a) grava/atualiza `target_dir/AGENTS.md` substituindo o conteúdo entre `LIFECYCLE_BEGIN`/`LIFECYCLE_END` se já existir (regex `re.DOTALL`, mesmo padrão de `_write_agents_block`), ou anexando o bloco no fim do arquivo (criando o AGENTS.md com um cabeçalho mínimo se ele não existir) se as marcações ainda não estiverem lá — NUNCA apague texto de fora dos delimitadores nem o bloco de `compiler.py` (`AGENTS_BEGIN`/`AGENTS_END`), que pode coexistir no mesmo arquivo; (b) grava `target_dir/.harness/LIFECYCLE.md` com `render_lifecycle_detail()`, criando `.harness/` se preciso. Retorna os dois `Path`. Crie `tests/test_lifecycle.py`: `render_lifecycle_block` contém as 16 referências numeradas (ou pelo menos os marcos: passo 1, passo 6 'escolher exatamente UMA feature', passo 11 verificação, passo 16 'deixar caminho limpo') e cita `.harness/LIFECYCLE.md`; `install_lifecycle` em AGENTS.md inexistente cria o arquivo com o bloco; em AGENTS.md com texto manual PRESERVA o texto manual e insere o bloco; rodar `install_lifecycle` DUAS VEZES seguidas não duplica o bloco (idempotente) e não deixa dois `LIFECYCLE_BEGIN`; AGENTS.md que já tem o bloco de `compiler.py` (`AGENTS_BEGIN`/`AGENTS_END`) com texto dentro PRESERVA esse bloco intacto depois de `install_lifecycle` rodar. Não toque em `cli.py`, `compiler.py` nem em nenhum outro módulo. Não faça refatoração fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests/test_lifecycle.py -q` — verde
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — suíte inteira verde
  - [ ] `python -m ruff check src/harness/lifecycle.py tests/test_lifecycle.py` — limpo

---

### [SUBAGENTE 04] - `templates.py`: `claude-progress.md` + `init.sh`/`init.ps1`
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Criar `src/harness/templates.py` — gera o template inicial de `claude-progress.md` a partir do contrato compilado e os scripts `init.sh`/`init.ps1` a partir do profile (instalação de deps + health check via test_command).
- **📂 Escopo de Arquivos:**
  - Ler: `src/harness/analyzer.py` (formato de `repo-profile.json`: `package_manager.value` ∈ {npm, pnpm, yarn, uv, poetry}, `test_command.value`), `src/harness/contract.py` (formato de `feature_list.json`), `ROADMAP.md` (seção Fase 2, item "Templates `claude-progress.md` + `init.*`")
  - Modificar (criar): `src/harness/templates.py`, `tests/test_templates.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, crie `src/harness/templates.py` (docstring PT-BR). Função `render_progress_template(feature_list: dict) -> str` retorna o conteúdo inicial de `claude-progress.md`: título, uma tabela/lista com cada feature de `feature_list['features']` (id, desc resumida, status inicial 'pending' já que `passes` vem `false`), e uma seção 'Última atualização' vazia pronta para o agente preencher durante a sessão (é estado RUNTIME-MUTÁVEL — este módulo só gera o esqueleto inicial, nunca deve sobrescrever um `claude-progress.md` que já existe). Função `render_init_scripts(profile: dict) -> tuple[str, str]` retorna `(init_sh, init_ps1)`: comando de instalação de deps por `(profile.get('package_manager') or {}).get('value')` (NÃO use `profile.get('package_manager', {})` sozinho — a chave `package_manager` sempre existe no `repo-profile.json` real, com valor `None` explícito quando não detectado, então o default `{}` do `.get` nunca entra em jogo e `None.get(...)` lançaria `AttributeError`; mesmo cuidado para `profile.get('test_command')`) — `npm ci` (npm), `pnpm install --frozen-lockfile` (pnpm), `yarn install --frozen-lockfile` (yarn), `uv sync` (uv), `poetry install` (poetry), ou comentário 'nenhum package manager detectado — pule esta etapa' se ausente/None — seguido do health check: rodar `profile.get('test_command', {}).get('value')` se presente (senão comentário 'nenhum test_command detectado'). `init.sh` em bash (`#!/usr/bin/env bash`, `set -e`), `init.ps1` em PowerShell (`$ErrorActionPreference = 'Stop'`) — mesmo conteúdo semântico nas duas linguagens. Função `install_templates(target_dir: Path) -> list[Path]`: grava `target_dir/claude-progress.md` APENAS SE o arquivo ainda não existir (é estado runtime — recompilar não pode apagar progresso já registrado); SEMPRE (re)grava `target_dir/init.sh` e `target_dir/init.ps1` (determinísticos, sem estado). Retorna a lista de paths escritos nesta chamada (se `claude-progress.md` já existia, ele NÃO entra na lista). Crie `tests/test_templates.py`: `render_progress_template` lista todas as features com status pending; `render_init_scripts` para profile com `package_manager npm` gera `npm ci` nos dois scripts; para profile com a chave `package_manager` AUSENTE gera o comentário; para profile com `package_manager` valor `None` EXPLÍCITO (`{\"package_manager\": None, ...}`) também gera o comentário sem lançar `AttributeError` (regressão do achado da revisão); `install_templates` em diretório vazio cria os 3 arquivos; rodar `install_templates` de novo com um `claude-progress.md` já customizado pelo usuário/agente PRESERVA esse conteúdo (não é sobrescrito), mas `init.sh`/`init.ps1` são regenerados com o profile novo. Não toque em `cli.py` nem em outros módulos. Não faça refatoração fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests/test_templates.py -q` — verde
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — suíte inteira verde
  - [ ] `python -m ruff check src/harness/templates.py tests/test_templates.py` — limpo

---

### [SUBAGENTE 05] - `session_start.py`: hook SessionStart injeta estado
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Criar `src/harness/session_start.py` — hook do evento `SessionStart` que injeta no início da sessão um resumo do progresso (`claude-progress.md`), a feature ativa/pendente (`feature_list.json`) e o `git log` recente, para o agente nascer sabendo onde parou.
- **📂 Escopo de Arquivos:**
  - Ler: `src/harness/compiler.py` (`_hook_entry`, formato de registro de hook em `settings.json` — o evento aqui é `SessionStart`, não `PreToolUse`, então o schema de saída do hook é diferente; ANTES de implementar, confirme o schema JSON exato que o Claude Code espera de um hook `SessionStart` — não assuma o mesmo formato de `PreToolUse`), `ROADMAP.md` (seção Fase 2, item "Hook SessionStart")
  - Modificar (criar): `src/harness/session_start.py`, `tests/test_session_start.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, ANTES de escrever código, confirme o schema JSON de saída esperado por um hook do evento `SessionStart` do Claude Code (é diferente do `PreToolUse` — não tem `permissionDecision`; o mecanismo real de injeção de contexto usa `hookSpecificOutput.additionalContext`, ou texto simples no stdout, dependendo da versão documentada — verifique antes de codar, não assuma). Depois, crie `src/harness/session_start.py` (docstring PT-BR) com `render_session_start_hook() -> str`: script standalone stdlib (json, subprocess, sys — sem `import harness`) que, ao rodar: (1) lê `.harness/feature_list.json` relativo ao cwd recebido no payload (se ausente, contexto injetado só diz 'nenhum contrato ativo'); calcula a primeira feature com `passes: false` como 'feature ativa/pendente' (se todas `true` ou lista vazia, diz 'nenhuma feature pendente'); (2) lê as últimas ~20 linhas de `claude-progress.md` se existir; (3) roda `git log -n 5 --oneline` via `subprocess.run` (cwd = cwd recebido, timeout curto, `capture_output=True`) e usa a saída se o comando funcionar, ou omite essa seção silenciosamente se falhar (repo sem git, por exemplo — não pode quebrar a sessão por isso). Monta essas 3 partes num texto de contexto e imprime no formato de saída confirmado no passo anterior. Função `install_session_start(target_dir: Path) -> Path`: grava `target_dir/.harness/hooks/session_start.py` e registra no `target_dir/.claude/settings.json` em `hooks.SessionStart` (estrutura equivalente à de `hooks.PreToolUse` mas sob a chave do evento certo — confirme o formato exato, inclusive se precisa de `matcher`), usando merge não-destrutivo próprio (chave nova `session_start_hook_command` em `.harness/compiled-state-session.json` — MESMO arquivo usado por `session_permissions.py`/`boundary_guard.py`, NUNCA `.harness/compiled-state.json` do mecanismo antigo, que `compiler.py::_write_state` reconstrói do zero e apagaria essa chave — preservando as chaves de outras tarefas desta fase que já possam estar lá). Crie `tests/test_session_start.py`: rodar o script gerado via subprocess com payload `{\"cwd\": <tmp_path>}` (sem `feature_list.json`) → contexto menciona 'nenhum contrato ativo'; com `feature_list.json` de 2 features (uma `passes:true`, outra `false`) → contexto cita a `false` como pendente; com `claude-progress.md` presente → seu conteúdo aparece no contexto; `install_session_start` registra o hook em `settings.json` sob o evento certo e é idempotente (rodar 2x não duplica a entrada). Não toque em `cli.py`, `compiler.py` nem em outros módulos. Não faça refatoração fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests/test_session_start.py -q` — verde
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — suíte inteira verde
  - [ ] `python -m ruff check src/harness/session_start.py tests/test_session_start.py` — limpo

---

### [SUBAGENTE 06] - CLI `compile-session` + skill `plan` chama a compilação da Fase 2
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Expor um único subcomando `harness compile-session --dir` que compila, em sequência, os 5 artefatos da Fase 2 (permissions, boundary guard, lifecycle, templates, SessionStart), e atualizar a skill `/harness-creator:plan` para chamá-lo logo após `compile-contract`.
- **📂 Escopo de Arquivos:**
  - Ler: `src/harness/cli.py` (padrão dos subcomandos existentes: import lazy dentro do branch, `--dir` default '.', JSON indent 2 no stdout, `erro: ` no stderr + exit 1), `src/harness/session_permissions.py`, `src/harness/boundary_guard.py`, `src/harness/lifecycle.py`, `src/harness/templates.py`, `src/harness/session_start.py` (assinaturas exatas das funções `compile_session_permissions`/`install_boundary_guard`/`install_lifecycle`/`install_templates`/`install_session_start` produzidas pelas 5 tarefas anteriores), `skills/plan/SKILL.md` (Passo 6 atual — compile-contract)
  - Modificar: `src/harness/cli.py`, `tests/test_cli.py`, `skills/plan/SKILL.md`
- **🤖 Prompt para o Claude Code:**
  > "Claude, adicione UM subcomando novo a `src/harness/cli.py`, seguindo o padrão exato dos existentes (import lazy dentro do branch `if args.command == ...`, flag `--dir` default '.'): `harness compile-session --dir X`. Implementação: chama, em sequência, `session_permissions.compile_session_permissions(target_dir)`, `boundary_guard.install_boundary_guard(target_dir)`, `lifecycle.install_lifecycle(target_dir)`, `templates.install_templates(target_dir)`, `session_start.install_session_start(target_dir)`. Se `compile_session_permissions` levantar `FileNotFoundError` (sem `feature_list.json` — sem contrato compilado ainda), capture e imprima no stderr `erro: ` + a mensagem, exit 1, SEM rodar os passos seguintes (a ordem importa: sem contrato, não tem sentido compilar guard/templates). Em sucesso, imprima no stdout um JSON `{\"settings\": \"<path .claude/settings.json>\", \"agents_md\": \"<path AGENTS.md>\", \"lifecycle_detail\": \"<path .harness/LIFECYCLE.md>\", \"templates\": [<paths escritos por install_templates>], \"session_start_hook\": \"<path .harness/hooks/session_start.py>\"}` (ajuste as chaves exatas ao que cada função retorna de fato — confira os tipos de retorno reais antes de montar o dict, não invente). Exit 0. Depois, edite `skills/plan/SKILL.md`: insira um novo 'Passo 7 — Compilar a sessão autônoma (Fase 2)' IMEDIATAMENTE depois do Passo 6 atual (compile-contract), instruindo rodar `python -m harness.cli compile-session --dir <alvo>` e mostrar ao usuário os artefatos gerados (settings.json com a superfície allow do contrato, boundary_guard.py ativo, AGENTS.md com o lifecycle de 16 passos, claude-progress.md/init.* gerados, hook SessionStart registrado); mencione explicitamente que o runtime floor (segredos, rede não planejada, `git push`) nunca vira `allow` mesmo depois desta compilação. NÃO renumere passos anteriores ao 6. Adicione em `tests/test_cli.py` (arquivo já existente — só ANEXE, não reescreva os testes do subcomando `analyze`/`compile-contract` já lá) um teste do subcomando `compile-session`: cenário feliz (contrato + profile já compilados em `tmp_path` via chamadas diretas às funções de `contract.py`/`analyzer.py` antes de invocar a CLI) → exit 0 e os artefatos existem em disco; cenário sem `feature_list.json` → exit 1 e stderr com 'erro: '. Não altere os subcomandos `run`/`compile`/`audit`/`analyze`/`compile-contract` já existentes. Nenhuma refatoração fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests/test_cli.py -q` — verde
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — suíte inteira verde
  - [ ] `python -m ruff check src/harness/cli.py tests/test_cli.py` — limpo
  - [ ] `Select-String -Path skills/plan/SKILL.md -Pattern "compile-session"` retorna match
  - [ ] `Select-String -Path skills/plan/SKILL.md -Pattern "^## Passo 7"` retorna exatamente 1 linha (passo novo inserido sem duplicar numeração)

---

### [SUBAGENTE 07] - E2E sintético: fluxo completo contrato → sessão compilada → boundary_guard em ação
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Provar, num repo sintético em `tmp_path` (sem custo de tokens reais), que `analyze → spec/Plans aprovado → compile-contract → compile-session` produz um `settings.json`/`boundary_guard.py` coerentes, e que o hook gerado de fato nega uma ação fora do raio e permite uma dentro — end-to-end via subprocess, no mesmo espírito de `tests/e2e/test_contract_flow.py` (Fase 1).
- **📂 Escopo de Arquivos:**
  - Ler: `tests/e2e/test_contract_flow.py` (padrão E2E do repo: subprocess com `python -m harness.cli ...`, env `PYTHONPATH=src`, cwd controlado), `tests/e2e/conftest.py` (só como referência de estilo — não usar a fixture `api_project`, que é da cobaia .NET real), `src/harness/cli.py` (subcomando `compile-session` novo)
  - Modificar (criar): `tests/e2e/test_boundary_flow.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, crie `tests/e2e/test_boundary_flow.py` seguindo o padrão de subprocess de `tests/e2e/test_contract_flow.py` (invocar `python -m harness.cli ...` com env `PYTHONPATH=src`, cwd controlado, sem rede/Docker). Cenário num repo sintético Python criado em `tmp_path` (um `pyproject.toml` mínimo com pytest configurado, um `src/app.py`, um `tests/test_app.py`): (1) `analyze --dir` → exit 0, `.harness/repo-profile.json` gravado; (2) escreva `.harness/work/demo/spec.md` JÁ aprovado (`approved_by`/`approved_at` preenchidos) e `Plans.md` com 1 tarefa `## [T-01] ...` tocando `src/app.py` com `verify: pytest tests/test_app.py -q`; `compile-contract --dir --slug demo` → exit 0, `feature_list.json` com 1 feature; (3) `compile-session --dir` → exit 0; leia `.claude/settings.json` e confirme que `permissions.allow` contém `Edit(src/app.py)` e `Bash(pytest tests/test_app.py -q)`, e NÃO contém nenhuma variação de `git push`; confirme que `.harness/hooks/boundary_guard.py` existe e está registrado em `hooks.PreToolUse`; (4) invoque o `boundary_guard.py` gerado DIRETAMENTE via subprocess com payload JSON no stdin (mesmo padrão de `_run_hook` de `tests/test_compiler.py`): Edit em `src/app.py` → `allow`; Edit em `src/outro.py` (fora do contrato) → `deny`; Bash `pytest tests/test_app.py -q` → `allow`; Bash `git push origin main` → `deny`; Edit em `tests/test_app.py` SEM esse arquivo estar em `files[]` da tarefa → `deny` (proteção de teste). Use os formatos exatos que `contract.py`/`session_permissions.py`/`boundary_guard.py` já definem — não invente formato novo. **CENÁRIO ADICIONAL (achado da revisão — colisão com o mecanismo antigo):** antes do passo (3), rode também `compile --dir` (subcomando ANTIGO, `harness.yaml` mínimo com `enforce_tdd: true`) no mesmo repo sintético, confirmando que isso gera a entrada de hook `guard_tests.py` em `hooks.PreToolUse`; DEPOIS rode `compile-session --dir` e confirme que a entrada de `guard_tests.py` foi REMOVIDA de `hooks.PreToolUse` (só sobra a do `boundary_guard.py`) — prova de que os dois mecanismos não coexistem duplicando gate de teste. **CENÁRIO ADICIONAL (floor sem contrato):** num diretório `tmp_path` SEM `.harness/feature_list.json` nenhum (nunca rodou `compile-contract`), invoque o `boundary_guard.py` (instalado isoladamente, ou copie o script gerado por `render_boundary_guard()` direto) com Bash `git push origin main` → `deny`, e Edit em `.env` → `deny` (prova de que o runtime floor nunca depende de contrato ativo). Não modifique `conftest.py` nem os E2E existentes."
- **🧪 Critério de Validação (DoD):**
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests/e2e/test_boundary_flow.py -v` — verde
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — suíte inteira verde
  - [ ] `python -m ruff check tests/e2e/test_boundary_flow.py` — limpo

---

### [SUBAGENTE 08] - Docs: README/GUIDE/CHANGELOG/versão da Fase 2
> ✅ CONCLUÍDO
- **🎯 Objetivo:** Documentar os artefatos novos (`compile-session`, boundary_guard, lifecycle, templates, SessionStart) e registrar a versão seguinte EM TODOS os arquivos de versão do repo, no mesmo tom PT-BR direto já usado.
- **📂 Escopo de Arquivos:**
  - Ler: `README.md`, `GUIDE.md`, `CHANGELOG.md`, `.claude-plugin/plugin.json`, `pyproject.toml` (versão atual `0.11.0`), `ROADMAP.md` (Fase 2)
  - Modificar: `README.md`, `GUIDE.md`, `CHANGELOG.md`, `.claude-plugin/plugin.json`, `pyproject.toml`
- **🤖 Prompt para o Claude Code:**
  > "Claude, atualize a documentação para a Fase 2 do ROADMAP (Execução Autônoma no Raio de Impacto), mantendo o tom existente (PT-BR, direto, sem marketing): (1) README.md — mencione o subcomando `harness compile-session` na linha de CLI, e troque a linha de versão no topo (`**v0.11.0** · [CHANGELOG](CHANGELOG.md)`) para `**v0.12.0**`; (2) GUIDE.md — nova seção curta descrevendo que, depois da aprovação do contrato (seção 'Trabalhar por contrato' já existente), rodar `compile-session` compila a superfície de permissions do raio de impacto, o `boundary_guard.py` único, o lifecycle de 16 passos no AGENTS.md, os templates `claude-progress.md`/`init.*` e o hook SessionStart — deixe claro que o runtime floor (segredos, rede não planejada, `git push`) nunca vira `allow`. Insira essa seção logo depois da seção 'Trabalhar por contrato' e RENUMERE as seções seguintes sem deixar número duplicado nem buraco (confira com `Select-String -Path GUIDE.md -Pattern \"^## \"` antes e depois); (3) CHANGELOG.md — entrada 0.12.0 no topo, mesmo formato das existentes, listando: session_permissions (allow compilado do contrato), boundary_guard.py (dispatcher único PreToolUse com proteção de enfraquecimento de teste), lifecycle.py (16 passos no AGENTS.md), templates.py (claude-progress.md/init.*), session_start.py (hook SessionStart), subcomando `compile-session`; (4) `.claude-plugin/plugin.json` — version para 0.12.0; (5) `pyproject.toml` — `version = \"0.12.0\"`. Não altere ARCHITECTURE.md nem ROADMAP.md. Nenhuma mudança de código."
- **🧪 Critério de Validação (DoD):**
  - [ ] `python -c "import json; print(json.load(open('.claude-plugin/plugin.json'))['version'])"` imprime `0.12.0`
  - [ ] `python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"` imprime `0.12.0`
  - [ ] `Select-String -Path README.md -Pattern "compile-session"` e `Select-String -Path README.md -Pattern "v0.12.0"` retornam match
  - [ ] `Select-String -Path CHANGELOG.md -Pattern "0.12.0"` retorna match
  - [ ] `Select-String -Path GUIDE.md -Pattern "^## "` não retorna números de seção duplicados
  - [ ] `$env:PYTHONPATH = "src"; python -m pytest tests -q` — verde (garantia de que nada de código foi tocado)

---

### [SUBAGENTE 09] - E2E dogfood real: AMPLIA `test_contract_dogfood.py` com o boundary_guard em ação
> ✅ CONCLUÍDO — dogfood real rodado, evidência em tests/e2e/evidence/fase2-dogfood-boundary-guard.md
> ⚠️ Gate final da Fase 2, opt-in (`HARNESS_E2E_DOGFOOD=1`), mesmo padrão de custo/skip do [SUBAGENTE 08] da Fase 1. NÃO reescreve o teste da Fase 1 do zero — ADICIONA um teste novo no MESMO arquivo, reaproveitando a fixture `api_project` e o cenário `dogfood-document-digits` já existentes, e prova as DUAS coisas exigidas pelo Gate de Encerramento por Fase do ROADMAP.md: zero regressão do fluxo da Fase 1 (analyze→spec/Plans→compile-contract continua funcionando) e a novidade real da Fase 2 (`compile-session` + `boundary_guard.py` negando uma ação fora da superfície numa sessão `claude -p` headless real, e a tarefa aprovada passando sem prompt).
- **🎯 Objetivo:** Provar de ponta a ponta, na cobaia real (a cobaia .NET externa), que uma sessão real do Claude, agindo dentro do contrato aprovado, tem a tarefa aprovada implementada e verificada (zero regressão do mecanismo da Fase 1), E que uma tentativa de ação fora da superfície enumerada (editar um arquivo fora de `files[]`, ou `git push`) é negada de verdade pelo `boundary_guard.py` compilado — evidenciado por `permission_denials` na resposta JSON do `claude -p`.
- **📂 Escopo de Arquivos:**
  - Ler: `tests/e2e/test_contract_dogfood.py` (teste da Fase 1 completo: fixture `api_project`, `SPEC_MD_TEMPLATE`/`PLANS_MD`/`HARNESS_YAML`/`CLAUDE_PROMPT` do cenário `dogfood-document-digits`, `_run_cli`, `_run_dotnet_test`, `_parse_trx`, `_write_evidence` — REUTILIZE essas funções/constantes, não duplique), `tests/e2e/conftest.py` (fixture `api_project`), `src/harness/cli.py` (subcomando `compile-session` novo), `src/harness/boundary_guard.py`, `src/harness/session_permissions.py`
  - Modificar: `tests/e2e/test_contract_dogfood.py` (ADICIONAR uma função de teste nova — não tocar na função `test_contract_dogfood_document_digits` já existente nem no restante do arquivo além do necessário para reuso)
- **🤖 Prompt para o Claude Code:**
  > "Claude, ADICIONE uma nova função de teste em `tests/e2e/test_contract_dogfood.py` (não crie um arquivo novo, não altere `test_contract_dogfood_document_digits` já existente — REUTILIZE `SPEC_MD_TEMPLATE`, `PLANS_MD`, `VALIDATOR_REL`, `SLUG`, `_run_cli`, `_run_dotnet_test`, `_parse_trx`, `api_project`, e o mesmo `pytestmark`/fixture `_require_toolchain` já no topo do arquivo). Chame a nova função `test_contract_dogfood_boundary_guard_denies_out_of_scope`. Passos: (1) repita os passos 1-5 do teste existente (TDD real do `Document_with_letters_fails`, `analyze`, escrever `spec.md`/`Plans.md` do contrato `dogfood-document-digits` JÁ aprovado com a tarefa T-01 tocando só `Cobaia/Validators/CustomerValidators.cs`, `compile-contract`, e compilar o `harness.yaml` de governança igual ao teste existente); (2) NOVO: rode `compile-session --dir <cobaia>` via `_run_cli` (adicione ao subcomando, exit 0) — isso instala `boundary_guard.py` e a superfície de permissions compilada do contrato; (3) invoque `claude -p` headless real com um prompt que peça DUAS coisas na mesma sessão: (a) implementar a tarefa T-01 exatamente como no teste existente (documento só dígitos), rodando `dotnet test Cobaia.Tests` você mesmo antes de terminar; (b) DEPOIS de terminar T-01, tentar também editar um arquivo FORA do contrato — por exemplo `Cobaia/Program.cs` (adicionar um comentário trivial) — e observar que a tentativa é negada; o prompt deve deixar claro que essa segunda tentativa é esperada falhar e não deve ser insistida. (4) Assert que `out['is_error'] is False` E que `out.get('permission_denials')` não é vazio/None (prova que o boundary_guard negou algo de verdade — NÃO confie em texto, confie no campo estruturado). (5) PROVA FINAL igual ao teste existente: `dotnet test Cobaia.Tests` de novo fora do Claude, `returncode == 0`, incluindo `Document_with_letters_fails` e os 3 testes pré-existentes (zero regressão do mecanismo da Fase 1 na mesma cobaia). (6) Confirme também, via `git`/leitura de arquivo, que `Cobaia/Program.cs` NÃO foi modificado (a negação realmente bloqueou a escrita, não só o texto da resposta disse que bloqueou). (7) Evidência em arquivo NOVO (não sobrescreva a da Fase 1): `tests/e2e/evidence/fase2-dogfood-boundary-guard.md`, com seções `## Regressão (Fase 1 na mesma cobaia)`, `## Negação da ação fora do raio (boundary_guard)`, `## Diff aplicado (T-01)`, `## Execução do agente` (incluindo o valor de `permission_denials`) — grave SEMPRE (sucesso ou falha), igual ao padrão de `_write_evidence` já usado no teste existente (pode reaproveitar essa função com um path/seções diferentes, ou uma cópia pequena adaptada). Não modifique `conftest.py`, não toque na função de teste da Fase 1, não sobrescreva `tests/e2e/evidence/fase1-dogfood-document-digits.md`."
- **🧪 Critério de Validação (DoD):**
  - [ ] Sem `HARNESS_E2E_DOGFOOD` setado: `$env:PYTHONPATH = "src"; python -m pytest tests/e2e/test_contract_dogfood.py -v` — as 2 funções (Fase 1 + Fase 2) aparecem `skipped`, suíte não quebra
  - [ ] Com o ambiente real disponível: `$env:HARNESS_E2E_DOGFOOD = "1"; $env:PYTHONPATH = "src"; python -m pytest tests/e2e/test_contract_dogfood.py -v -s` — AMBAS as funções passam (a da Fase 1 continua verde — zero regressão — e a nova da Fase 2 também)
  - [ ] `python -m ruff check tests/e2e/test_contract_dogfood.py` — limpo
  - [ ] `Test-Path tests/e2e/evidence/fase2-dogfood-boundary-guard.md` retorna `True` após a execução real, com as 4 seções (`Select-String -Path tests/e2e/evidence/fase2-dogfood-boundary-guard.md -Pattern "^## "` retorna 4 linhas)
  - [ ] `Test-Path tests/e2e/evidence/fase1-dogfood-document-digits.md` continua `True` e inalterado (regressão de evidência da Fase 1 não foi apagada)
  - [ ] Confirmação humana pós-execução: `git -C <caminho da cobaia .NET> diff --stat` continua vazio (a tarefa só editou a CÓPIA em tmp, nunca o repo original)
